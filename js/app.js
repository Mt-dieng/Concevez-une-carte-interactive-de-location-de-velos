//ajout d'évenement...

document.addEventListener("DOMContentLoaded", function(event) 
{ 
// Instanciation de l'objet 
  let sliders-list = new Slider(sliders-list);
  


let flecheGauche = getElementById('fleche-gauche');
 // Qd je clique sur fleche gauche, je déclenche la méthode défiler gauche de mon objet slider1 
 let flecheDroite = getElementById('fleche-droite');
    //do work 
});